/**
 * Coupon definitions. The key is the code entered by customers and the
 * value is the percentage discount (e.g. 0.20 = 20% off). Feel free to
 * modify or extend this list. All codes are case insensitive.
 */
export const coupons: Record<string, number> = {
  HALO10: 0.10,
  HALO20: 0.20,
  HALO30: 0.30,
};